<div class="space-y-4">
    <div class="flex items-center cursor-pointer flex justify-between text-indigo-500">
        <div class="hover:underline underline-offset-8 text-[1.7rem] font-bold"> <%= @title %> </div>
        <%= bold_arrow_compoenent twcss: @close && "-rotate-90"%>
    </div>
    <% if @details %>
        <div class="text-[1.4rem]">
            <%= @details %>
        </div>
    <% end %>
</div>